name: Test Nim bindings

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

env:
  NIM_CC: vcc

jobs:
  build-and-test:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Add msbuild to PATH
        uses: microsoft/setup-msbuild@v2
        if: runner.os == 'Windows'

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      # - name: Setup Zig
      #   uses: goto-bus-stop/setup-zig@v2
      
      # - name: Add zigbuild
      #   run: cargo install cargo-zigbuild 

      - name: Run compile script
        run: ./scripts/compile.sh nim
        shell: bash

      - name: Install Nim (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Invoke-WebRequest -Uri https://nim-lang.org/download/nim-1.6.10_x64.zip -OutFile nim.zip
          Expand-Archive -Path nim.zip -DestinationPath .
          Move-Item -Path nim-1.6.10 -Destination "$env:USERPROFILE\.choosenim\toolchains\nim-1.6.10"
          echo "$env:USERPROFILE\.nimble\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          echo "$env:USERPROFILE\.choosenim\toolchains\nim-1.6.10\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Install Nim (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          curl https://nim-lang.org/choosenim/init.sh -sSf | sh -s -- -y
          echo "$HOME/.nimble/bin" >> $GITHUB_PATH
          choosenim stable

      - name: Set NIM_CC to vcc on Windows
        if: runner.os == 'Windows'
        run: echo "NIM_CC=vcc" >> $env:GITHUB_ENV    
      - name: Install Nimble dependencies
        working-directory: bindings/nim/nim_code
        run: nimble install -Y

      - name: Test Nim project
        working-directory: bindings/nim/nim_code
        run: nimble test -Y --verbose
